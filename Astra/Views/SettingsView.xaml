<UserControl x:Class="Astra.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource TextBrush}"
             MinWidth="400">
    <StackPanel Margin="12">
        <TextBlock Text="Display name" Margin="0,0,0,4" />
        <TextBox Text="{Binding Settings.DisplayName, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Text="Used to label exported recap files." FontSize="11" Opacity="0.7" Margin="0,4,0,0" />
    </StackPanel>
</UserControl>
